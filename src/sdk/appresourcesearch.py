"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from jsonpath import JSONPath
from sdk import utils
from sdk._hooks import HookContext
from sdk.models import errors, operations, shared
from sdk.types import BaseModel, OptionalNullable, UNSET
from sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Dict, List, Mapping, Optional, Union, cast


class AppResourceSearch(BaseSDK):
    def search_app_resource_types(
        self,
        *,
        request: Union[
            shared.SearchAppResourceTypesRequest,
            shared.SearchAppResourceTypesRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[operations.C1APIAppV1AppResourceSearchSearchAppResourceTypesResponse]:
        r"""Search App Resource Types

        Search app resources based on filters specified in the request body.

        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, shared.SearchAppResourceTypesRequest)
        request = cast(shared.SearchAppResourceTypesRequest, request)

        req = self._build_request(
            method="POST",
            path="/api/v1/search/app_resource_types",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", shared.SearchAppResourceTypesRequest
            ),
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="c1.api.app.v1.AppResourceSearch.SearchAppResourceTypes",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=["4XX", "5XX"],
            retry_config=retry_config,
        )

        def next_func() -> (
            Optional[
                operations.C1APIAppV1AppResourceSearchSearchAppResourceTypesResponse
            ]
        ):
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])
            next_cursor = JSONPath("$.nextPageToken").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None

            return self.search_app_resource_types(
                request=shared.SearchAppResourceTypesRequest(
                    app_ids=request.app_ids,
                    app_user_ids=request.app_user_ids,
                    display_name=request.display_name,
                    exclude_resource_type_ids=request.exclude_resource_type_ids,
                    exclude_resource_type_trait_ids=request.exclude_resource_type_trait_ids,
                    page_size=request.page_size,
                    page_token=next_cursor,
                    query=request.query,
                    resource_type_ids=request.resource_type_ids,
                    resource_type_trait_ids=request.resource_type_trait_ids,
                ),
                retries=retries,
            )

        if utils.match_response(http_res, "200", "application/json"):
            return operations.C1APIAppV1AppResourceSearchSearchAppResourceTypesResponse(
                search_app_resource_types_response=unmarshal_json_response(
                    Optional[shared.SearchAppResourceTypesResponse], http_res
                ),
                status_code=http_res.status_code,
                content_type=http_res.headers.get("Content-Type") or "",
                raw_response=http_res,
                next=next_func,
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)

        raise errors.SDKError("Unexpected response received", http_res)

    async def search_app_resource_types_async(
        self,
        *,
        request: Union[
            shared.SearchAppResourceTypesRequest,
            shared.SearchAppResourceTypesRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[operations.C1APIAppV1AppResourceSearchSearchAppResourceTypesResponse]:
        r"""Search App Resource Types

        Search app resources based on filters specified in the request body.

        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, shared.SearchAppResourceTypesRequest)
        request = cast(shared.SearchAppResourceTypesRequest, request)

        req = self._build_request_async(
            method="POST",
            path="/api/v1/search/app_resource_types",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", shared.SearchAppResourceTypesRequest
            ),
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="c1.api.app.v1.AppResourceSearch.SearchAppResourceTypes",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=["4XX", "5XX"],
            retry_config=retry_config,
        )

        def next_func() -> (
            Optional[
                operations.C1APIAppV1AppResourceSearchSearchAppResourceTypesResponse
            ]
        ):
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])
            next_cursor = JSONPath("$.nextPageToken").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None

            return self.search_app_resource_types(
                request=shared.SearchAppResourceTypesRequest(
                    app_ids=request.app_ids,
                    app_user_ids=request.app_user_ids,
                    display_name=request.display_name,
                    exclude_resource_type_ids=request.exclude_resource_type_ids,
                    exclude_resource_type_trait_ids=request.exclude_resource_type_trait_ids,
                    page_size=request.page_size,
                    page_token=next_cursor,
                    query=request.query,
                    resource_type_ids=request.resource_type_ids,
                    resource_type_trait_ids=request.resource_type_trait_ids,
                ),
                retries=retries,
            )

        if utils.match_response(http_res, "200", "application/json"):
            return operations.C1APIAppV1AppResourceSearchSearchAppResourceTypesResponse(
                search_app_resource_types_response=unmarshal_json_response(
                    Optional[shared.SearchAppResourceTypesResponse], http_res
                ),
                status_code=http_res.status_code,
                content_type=http_res.headers.get("Content-Type") or "",
                raw_response=http_res,
                next=next_func,
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)

        raise errors.SDKError("Unexpected response received", http_res)

    def search_app_resources(
        self,
        *,
        request: Union[
            shared.SearchAppResourcesRequest, shared.SearchAppResourcesRequestTypedDict
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[operations.C1APIAppV1AppResourceSearchSearchAppResourcesResponse]:
        r"""Search App Resources

        Invokes the c1.api.app.v1.AppResourceSearch.SearchAppResources method.

        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, shared.SearchAppResourcesRequest)
        request = cast(shared.SearchAppResourcesRequest, request)

        req = self._build_request(
            method="POST",
            path="/api/v1/search/app_resources",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", shared.SearchAppResourcesRequest
            ),
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="c1.api.app.v1.AppResourceSearch.SearchAppResources",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=["4XX", "5XX"],
            retry_config=retry_config,
        )

        def next_func() -> (
            Optional[operations.C1APIAppV1AppResourceSearchSearchAppResourcesResponse]
        ):
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])
            next_cursor = JSONPath("$.nextPageToken").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None

            return self.search_app_resources(
                request=shared.SearchAppResourcesRequest(
                    app_id=request.app_id,
                    app_user_ids=request.app_user_ids,
                    exclude_deleted_resource_bindings=request.exclude_deleted_resource_bindings,
                    exclude_resource_ids=request.exclude_resource_ids,
                    exclude_resource_type_trait_ids=request.exclude_resource_type_trait_ids,
                    page_size=request.page_size,
                    page_token=next_cursor,
                    query=request.query,
                    refs=request.refs,
                    resource_ids=request.resource_ids,
                    resource_type_ids=request.resource_type_ids,
                    resource_type_trait_ids=request.resource_type_trait_ids,
                ),
                retries=retries,
            )

        if utils.match_response(http_res, "200", "application/json"):
            return operations.C1APIAppV1AppResourceSearchSearchAppResourcesResponse(
                search_app_resources_response=unmarshal_json_response(
                    Optional[shared.SearchAppResourcesResponse], http_res
                ),
                status_code=http_res.status_code,
                content_type=http_res.headers.get("Content-Type") or "",
                raw_response=http_res,
                next=next_func,
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)

        raise errors.SDKError("Unexpected response received", http_res)

    async def search_app_resources_async(
        self,
        *,
        request: Union[
            shared.SearchAppResourcesRequest, shared.SearchAppResourcesRequestTypedDict
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[operations.C1APIAppV1AppResourceSearchSearchAppResourcesResponse]:
        r"""Search App Resources

        Invokes the c1.api.app.v1.AppResourceSearch.SearchAppResources method.

        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, shared.SearchAppResourcesRequest)
        request = cast(shared.SearchAppResourcesRequest, request)

        req = self._build_request_async(
            method="POST",
            path="/api/v1/search/app_resources",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", shared.SearchAppResourcesRequest
            ),
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="c1.api.app.v1.AppResourceSearch.SearchAppResources",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=["4XX", "5XX"],
            retry_config=retry_config,
        )

        def next_func() -> (
            Optional[operations.C1APIAppV1AppResourceSearchSearchAppResourcesResponse]
        ):
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])
            next_cursor = JSONPath("$.nextPageToken").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None

            return self.search_app_resources(
                request=shared.SearchAppResourcesRequest(
                    app_id=request.app_id,
                    app_user_ids=request.app_user_ids,
                    exclude_deleted_resource_bindings=request.exclude_deleted_resource_bindings,
                    exclude_resource_ids=request.exclude_resource_ids,
                    exclude_resource_type_trait_ids=request.exclude_resource_type_trait_ids,
                    page_size=request.page_size,
                    page_token=next_cursor,
                    query=request.query,
                    refs=request.refs,
                    resource_ids=request.resource_ids,
                    resource_type_ids=request.resource_type_ids,
                    resource_type_trait_ids=request.resource_type_trait_ids,
                ),
                retries=retries,
            )

        if utils.match_response(http_res, "200", "application/json"):
            return operations.C1APIAppV1AppResourceSearchSearchAppResourcesResponse(
                search_app_resources_response=unmarshal_json_response(
                    Optional[shared.SearchAppResourcesResponse], http_res
                ),
                status_code=http_res.status_code,
                content_type=http_res.headers.get("Content-Type") or "",
                raw_response=http_res,
                next=next_func,
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.SDKError("API error occurred", http_res, http_res_text)

        raise errors.SDKError("Unexpected response received", http_res)
