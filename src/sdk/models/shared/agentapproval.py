"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import PlainValidator
from sdk import utils
from sdk.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from sdk.utils import validate_open_enum
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class AgentFailureAction(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The action to take if the agent fails to approve, deny, or reassign the task."""

    APPROVAL_AGENT_FAILURE_ACTION_UNSPECIFIED = (
        "APPROVAL_AGENT_FAILURE_ACTION_UNSPECIFIED"
    )
    APPROVAL_AGENT_FAILURE_ACTION_REASSIGN_TO_USERS = (
        "APPROVAL_AGENT_FAILURE_ACTION_REASSIGN_TO_USERS"
    )
    APPROVAL_AGENT_FAILURE_ACTION_REASSIGN_TO_SUPER_ADMINS = (
        "APPROVAL_AGENT_FAILURE_ACTION_REASSIGN_TO_SUPER_ADMINS"
    )
    APPROVAL_AGENT_FAILURE_ACTION_SKIP_POLICY_STEP = (
        "APPROVAL_AGENT_FAILURE_ACTION_SKIP_POLICY_STEP"
    )


class AgentMode(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The mode of the agent, full control, change policy only, or comment only."""

    APPROVAL_AGENT_MODE_UNSPECIFIED = "APPROVAL_AGENT_MODE_UNSPECIFIED"
    APPROVAL_AGENT_MODE_FULL_CONTROL = "APPROVAL_AGENT_MODE_FULL_CONTROL"
    APPROVAL_AGENT_MODE_CHANGE_POLICY_ONLY = "APPROVAL_AGENT_MODE_CHANGE_POLICY_ONLY"
    APPROVAL_AGENT_MODE_COMMENT_ONLY = "APPROVAL_AGENT_MODE_COMMENT_ONLY"


class AgentApprovalTypedDict(TypedDict):
    r"""The agent to assign the task to."""

    agent_failure_action: NotRequired[AgentFailureAction]
    r"""The action to take if the agent fails to approve, deny, or reassign the task."""
    agent_mode: NotRequired[AgentMode]
    r"""The mode of the agent, full control, change policy only, or comment only."""
    agent_user_id: NotRequired[str]
    r"""The agent user ID to assign the task to."""
    instructions: NotRequired[str]
    r"""Instructions for the agent."""
    policy_ids: NotRequired[Nullable[List[str]]]
    r"""The allow list of policy IDs to re-route the task to."""
    reassign_to_user_ids: NotRequired[Nullable[List[str]]]
    r"""The users to reassign the task to if the agent failure action is reassign to users."""


class AgentApproval(BaseModel):
    r"""The agent to assign the task to."""

    agent_failure_action: Annotated[
        Annotated[
            Optional[AgentFailureAction], PlainValidator(validate_open_enum(False))
        ],
        pydantic.Field(alias="agentFailureAction"),
    ] = None
    r"""The action to take if the agent fails to approve, deny, or reassign the task."""

    agent_mode: Annotated[
        Annotated[Optional[AgentMode], PlainValidator(validate_open_enum(False))],
        pydantic.Field(alias="agentMode"),
    ] = None
    r"""The mode of the agent, full control, change policy only, or comment only."""

    agent_user_id: Annotated[Optional[str], pydantic.Field(alias="agentUserId")] = None
    r"""The agent user ID to assign the task to."""

    instructions: Optional[str] = None
    r"""Instructions for the agent."""

    policy_ids: Annotated[
        OptionalNullable[List[str]], pydantic.Field(alias="policyIds")
    ] = UNSET
    r"""The allow list of policy IDs to re-route the task to."""

    reassign_to_user_ids: Annotated[
        OptionalNullable[List[str]], pydantic.Field(alias="reassignToUserIds")
    ] = UNSET
    r"""The users to reassign the task to if the agent failure action is reassign to users."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = [
            "agentFailureAction",
            "agentMode",
            "agentUserId",
            "instructions",
            "policyIds",
            "reassignToUserIds",
        ]
        nullable_fields = ["policyIds", "reassignToUserIds"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
